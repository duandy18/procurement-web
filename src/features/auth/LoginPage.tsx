export default function LoginPage() {
  return (
    <div className="page-card">
      <p className="eyebrow">Auth</p>
      <h1>登录页占位</h1>
      <p>第一刀只保留独立前端 shell。用户登录和权限模型后续按采购系统独立合同接入。</p>
    </div>
  );
}
